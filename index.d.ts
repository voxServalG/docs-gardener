/**
 * Command-line entry for docs-gardener.
 *
 * The package currently exposes its functionality through the `docs-gardener`
 * binary. Runtime modules remain JavaScript and are intentionally not declared
 * as a public import API yet.
 */
export {};
