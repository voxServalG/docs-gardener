# Project Overview

docs-gardener is an MCP tool for checking documentation against code behavior. It exposes a parallel two-layer scan (hard + soft), a unified fix, and per-document polish. Every scan envelope carries a hard render directive that agents must honor.

See the [documentation index](index.md) for the docs root.

The CLI entry point is `src/index.js`.
