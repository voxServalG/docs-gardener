# Project Overview

docs-gardener is an MCP tool for checking documentation against code behavior. It exposes a hard layer (code-decidable checks) and a soft layer (LLM-judged checks bundled by the tool).

See the [documentation index](index.md) for the docs root.

The CLI entry point is `src/index.js`.
